{
  "name": "personal-links",
  "version": "1.0.0",
  "scripts": {
    "start": "eleventy --serve",
    "build": "eleventy",
    "clean-build": "rm -r public/ & eleventy",
    "spellcheck": "mdspell 'src/*.html' 'src/*.md' '!**/node_modules/**/*' -a -r "
  },
  "author": "Iestyn Gage",
  "license": "MIT",
  "devDependencies": {
    "@11ty/eleventy": "^1.0.2",
    "markdown-spellcheck": "^1.3.1"
  },
  "dependencies": {
    "prettier": "^2.7.1"
  }
}
